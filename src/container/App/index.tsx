import React, { Fragment } from "react";
import classes from "./App.module.scss";
import { Routes } from "../Router";

export const App = () => {
  return (
    <Fragment>
      <Routes />
    </Fragment>
  );
};
